"use client";

const Ninth = () => {
  return (
    <div
      style={{
        maxWidth: "800px",
        margin: "40px auto",
        padding: "30px",
        borderRadius: "12px",
        backgroundColor: "#f0f4f8",
        boxShadow: "0 4px 12px rgba(0, 0, 0, 0.08)",
        fontFamily: "Segoe UI, sans-serif",
        fontSize: "18px",
        lineHeight: "1.6",
        color: "#333",
      }}
    >
      <p>
        ✅ Created a <strong>responsive sidebar navigation</strong> using Material-UI's Drawer component and included it as part of the <code>layout.js</code> so that the sidebar appears consistently across all pages.
      </p>
    </div>
  );
};

export default Ninth;
